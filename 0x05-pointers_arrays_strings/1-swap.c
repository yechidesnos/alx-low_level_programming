#include <stdio.h>

/*
 * swap_int - the function who permute two integer
 * @a - first parameter
 * @b - second parameter
 * return: the permute number
 * */
void swap_int(int *a, int *b);
{
	*a = b;
	*b = a;
}
