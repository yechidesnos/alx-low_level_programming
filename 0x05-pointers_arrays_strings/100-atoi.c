#include <stdio.h>
#include "main.h"

/**
 * _atoi - transtype function
 * @s: param
 * return: Always 0 (Success)
 */
int _atoi(char *s)
{
	int num;

	num = _atoi(s);
	printf("%d",num);
	return (0);
}
